import React from 'react';
import { Route, Routes } from 'react-router-dom';

function App() {
    return (
        <Routes>
            <Route path='/' element={} />
            <Route path='/login' element={} />
        </Routes>
    );
}

export default App;
